#!/bin/sh

# File for NetworkManager
NAME="NetworkManager"

main () {
    case "$1" in
    -start) NetworkManager;;
    -stop) killall NetworkManager;;
    -restart) 
	killall NetworkManager
	sleep 1
	NetworkManager;;
    -poll) return 0;;
    esac
}

main