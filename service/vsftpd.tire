#!/bin/sh

# File for vsftpd
NAME="vsftpd"

main () {
    case "$1" in
    -start) vsftpd &;;
    -stop) killall vsftpd;;
    -restart) 
	killall vsftpd
	sleep 1
	vsftpd &;;
    -poll)
	echo_color 3 ":: $NAME is running."
	return 0;; # doesn't make much sense for this service
    esac
}

main