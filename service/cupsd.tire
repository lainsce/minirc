#!/bin/sh

# File for cupsd
NAME="cupsd"

main () {
    case "$1" in
    -start) cupsd;;
    -stop) killall cupsd;;
    -restart) 
	killall cupsd
	sleep 1
	cupsd;;
    -poll)
	echo_color 3 ":: $NAME is running."
	return 0;; # doesn't make much sense for this service
    esac
}

main "$@"