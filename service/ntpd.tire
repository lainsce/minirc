#!/bin/sh

# File for ntpd
NAME="ntpd"

main () {
    case "$1" in
    -start) ntpd -s;;
    -stop) killall ntpd;;
    -restart) 
	killall ntpd
	sleep 1
	ntpd -s;;
    -poll)
	echo_color 3 ":: $NAME is running."
	return 0;; # doesn't make much sense for this service
    esac
}

main