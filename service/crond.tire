#!/bin/sh

# File for crond
NAME="crond"

main(){
	case "$1" in
		-start) crond
		;;
		-stop) killall crond
		;;
		-restart)
			"$0" -stop
			sleep 1
			"$0" -start
		;;
		-poll) pgrep -x "$NAME" > /dev/null 2>&1
		;;
	esac
}

main "$@"
