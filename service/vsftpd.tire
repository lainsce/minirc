#!/bin/sh

# File for vsftpd
NAME="vsftpd"

main () {
    case "$1" in
    -start) vsftpd &;;
    -stop) killall vsftpd;;
    -restart) 
	killall vsftpd
	sleep 1
	vsftpd &;;
    -poll) return 0;;
    esac
}

main